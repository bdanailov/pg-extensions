# pg-extensions

## Overview

This directory contains configurations and metadata for PostgreSQL extensions used in our platform services. Each extension has its own set of files that define its metadata, Docker build configurations, and deployment settings.

## Files
